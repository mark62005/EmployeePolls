import PropTypes from "prop-types";
import { connect } from "react-redux";

const Dashboard = () => {
    return (
        <div>
            Dashboard
        </div>
    );
};

Dashboard.propTypes = {

};

export default connect()(Dashboard);